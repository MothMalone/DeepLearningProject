# Notebooks

`final_experiment.ipynb` is the project notebook intended for Kaggle execution.
It contains the final training run, validation checkpoint selection, decoding
sweep, test prediction block, and report artifact export.

Historical notebooks are in `archive/notebooks/` and are not part of the main
workflow.
