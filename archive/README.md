# Archive

This directory keeps earlier notebooks, Vast.ai helpers, and local preprocessing
experiments so the history is recoverable without crowding the main project
flow.

The active notebook is `notebooks/final_experiment.ipynb`.
